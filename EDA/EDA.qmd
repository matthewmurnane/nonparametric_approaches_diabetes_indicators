---
title: "EDA"
format: pdf
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE,
                      warning = FALSE,
                      message = FALSE)
```

```{r}
library(tidyverse)
library(gridExtra)
library(ggthemes)
library(kableExtra)
library(readr)
library(dplyr)
library(car)
```

```{r}
diabeted_multiclass <- read_csv("../data/diabeted_multiclass.csv")
diabetes_binary_balanced <- read_csv("../data/diabetes_binary_balanced.csv")
```

```{r}
glimpse(diabeted_multiclass)
glimpse(diabetes_binary_balanced)
```
```{r}
p1 <- diabeted_multiclass %>% 
  ggplot()+
  geom_bar(aes(x=dia))
```

EDA for the Binary Logistic Regression Question
``` {r}
q1 <- diabetes_binary_balanced %>% select(Diabetes_binary, HvyAlcoholConsump, Income, Smoker, Age, PhysActivity, HeartDiseaseorAttack)

```